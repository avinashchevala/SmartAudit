/**
 * Babel Configuration
 * 
 * Babel configuration for React Native development.
 * Uses the official React Native preset for optimal compilation.
 */

module.exports = {
  // Use React Native's recommended preset for JavaScript/TypeScript compilation
  presets: ['module:@react-native/babel-preset'],
};
